#include <stdio.h>


int main() {
    int a=10;
    int b=2;
    int sum=a+b;
    int diff=a-b;
    int product=a*b;
    int remainder=a%b;
    printf("The sum is %d\n",sum);
    printf("The difference is %d\n",diff);
    printf("The product is %d\n",product);
    printf("The remainder is %d\n",remainder);
    return 0;
}